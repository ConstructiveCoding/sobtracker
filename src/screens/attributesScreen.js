/* @flow */

import React from 'react';
import { Text, View } from 'react-native';

import Strings from '../language/strings';

const AttributesScreen = props => (
  <View>
    <Text>{Strings.attributes}</Text>
  </View>
);

export default AttributesScreen;
